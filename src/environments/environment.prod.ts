// Production environment configuration
export const environment = {
  production: true,
  // Production backend URL (example); replace with real production endpoint.
  backendBaseUrl: 'https://car-dekho-backend-production.up.railway.app',
};
